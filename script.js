// Fungsi untuk menampilkan ucapan tambahan
function showMessage() {
    const message = document.getElementById("extraMessage");
    message.style.display = "block"; // Menampilkan pesan tambahan saat tombol diklik
}